[tool.poetry]
name = "store-keeper-api"
version = "0.1.0"
description = ""
authors = ["denis <denisergo18@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
